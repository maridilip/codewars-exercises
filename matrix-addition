function matrixAddition(a, b) {
  
  let res = [];
  for (let i = 0; i < a.length; i++) {
    res[i] = [];
    for (let j = 0; j < b.length; j++) {        
      res[i][j] = a[i][j] + b[i][j];
    }
  }
  
  return res;
}
